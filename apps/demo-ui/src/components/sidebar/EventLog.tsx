/**
 * EventLog — Task 1 stub. Task 2 of plan 06-04 wires this to useEvents() +
 * react-virtuoso (followOutput="auto" per RESEARCH Pitfall 3) with the chat
 * filter (D-07). Kept minimal here so Sidebar/App can compose without import
 * errors during the Task 1 commit.
 */
export function EventLog(): JSX.Element {
  return <div className="event-log event-log--stub" aria-live="polite" />;
}
