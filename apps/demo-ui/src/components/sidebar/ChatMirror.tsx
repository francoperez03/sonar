/**
 * ChatMirror — Task 1 stub. Task 2 of plan 06-04 wires this to useChats() +
 * react-virtuoso with bubble rendering. Kept minimal here so Sidebar/App can
 * compose without import errors during the Task 1 commit.
 */
export function ChatMirror(): JSX.Element {
  return <div className="chat-mirror chat-mirror--stub" aria-live="polite" />;
}
