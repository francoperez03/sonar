// Stub — real implementation in Phase 3.
import type { ITransport } from '@sonar/shared';
import { Message } from '@sonar/shared';

export const _placeholderTransportType = (t: ITransport) => t;
void Message;
console.log('@sonar/operator stub — implementation in Phase 3');
